
@{
    Layout = "~/Views/Shared/_Layout.cshtml";

}
    <!-- Filtros -->
@*     <section class="erp-card filters-card mb-4">
        <!-- Filtros -->
        <div class="section-head">
            <div>
                <h2 class="section-title">Filtros</h2>
                <p class="section-subtitle">Aplicá filtros para encontrar movimientos más rápido.</p>
            </div>
            <div class="d-flex gap-2 flex-wrap">
                <button class="btn btn-soft" onclick="limpiarFiltrosGasto()">
                    <i class="bi bi-arrow-clockwise me-2"></i>Limpiar
                </button>
            </div>
        </div>

        <div class="filters-body">
            <div class="row g-3">

                <!-- Desde -->
                <div class="col-md-2">
                    <label class="form-label">Desde</label>
                    <input type="date" class="form-control"
                           id="fechaDesde"
                           value="@ViewBag.FechaDesde"
                           onchange="aplicarFiltros()" />
                </div>

                <!-- Hasta -->
                <div class="col-md-2">
                    <label class="form-label">Hasta</label>
                    <input type="date" class="form-control"
                           id="fechaHasta"
                           value="@ViewBag.FechaHasta"
                           onchange="aplicarFiltros()" />
                </div>

                <!--los nombres de los rubros fijos se muestran primero  -->
                @{
                    var rubrosFijos = new[] { 34, 35 };

                    var rubrosOrdenados = rubros
                    .OrderByDescending(r => rubrosFijos.Contains(r.Id));
                }


                <!-- Rubro -->
                <div class="col-md-3">
                    <label class="form-label">Rubro</label>
                    <select class="form-select" id="rubroFilter" onchange="aplicarFiltros()">
                        <option value="">Todos</option>
                        @foreach (var rubro in rubrosOrdenados)
                        {
                            <option value="@rubro.Id"
                                    selected="@(ViewBag.FiltroRubroId?.ToString() == rubro.Id.ToString())">
                                @rubro.nombreRubro
                            </option>
                        }
                    </select>
                </div>

                <!-- Cuenta -->
                <div class="col-md-3">
                    <label class="form-label">Cuenta</label>
                    <select class="form-select" id="cuentaFilter" onchange="aplicarFiltros()">
                        <option value="">Todas</option>
                        @foreach (var cuenta in cuentas)
                        {
                            <option value="@cuenta.Id"
                                    selected="@(ViewBag.FiltroCuentaId?.ToString() == cuenta.Id.ToString())">
                                @cuenta.nombreCuenta
                            </option>
                        }
                    </select>
                </div>

            </div>
            <div class="quick-filters">
                <a href="#" class="quick-chip active">Hoy</a>
                <a href="#" class="quick-chip">Últimos 7 días</a>
                <a href="#" class="quick-chip">Este mes</a>
                <a href="#" class="quick-chip">Mes anterior</a>
                <a href="#" class="quick-chip">Solo Mercado Pago</a>
            </div>
        </div>
    </section> *@

@*     <div class="card border-0 shadow-sm rounded-4 mb-4">
        <div class="card-body p-4">
            <form class="row g-3 align-items-end" id="filtrosForm">
                <div class="col-md-6">
                    <label class="form-label small fw-bold mb-2" style="color: #6f42c1;">NOMBRE</label>
                    <input type="text" id="inputNombre" name="searchNombre" class="form-control bg-light border-0 rounded-3 py-2" value="@searchNombre" placeholder="Buscar por nombre...">
                </div>

                <div class="col-md-6">
                    <label class="form-label small fw-bold mb-2" style="color: #6f42c1;">NOMBRE FANTASÍA</label>
                    <input type="text" id="inputNombreFantasia" name="searchFantasia" class="form-control bg-light border-0 rounded-3 py-2" value="@searchFantasia" placeholder="Buscar por fantasía...">
                </div>

                <div class="col-md-12">
                    <button type="button" class="btn btn-sm btn-outline-primary rounded-3" onclick="limpiarFiltros()">
                        <i class="bi bi-arrow-clockwise me-1"></i> Limpiar
                    </button>
                    <span id="loadingIndicator" class="ms-2 d-none">
                        <span class="spinner-border spinner-border-sm me-2" style="color: #6f42c1;"></span>
                        <small>Buscando...</small>
                    </span>
                </div>
            </form>
        </div>
    </div> *@

    <!-- Contenedor de la tabla (se reemplaza por AJAX) -->
    <div id="clientes-content">
        @await Html.PartialAsync("_ClientesContent")
    </div>
</main>

